# 🛠️ Bash Scripting Suite for System Maintenance

**Author:** Pallab Pradhan  
**Objective:** Automate essential Linux system maintenance tasks using Bash scripting.

## 🚀 Features

- **Automated System Backup (`backup.sh`)**
  - Creates backups of critical directories and files.
  - Supports timestamped archives for easy retrieval.

- **System Update and Cleanup (`update_clean.sh`)**
  - Updates package lists and upgrades installed packages.
  - Cleans up unused packages and cache to free space.

- **Log Monitoring with Error Alerts (`log_monitor.sh`)**
  - Scans system logs for error patterns.
  - Sends alerts or logs issues for review.

- **Main Menu Interface (`menu.sh`)**
  - Simple, interactive menu to access all scripts.
  - Logs all actions for auditing and troubleshooting.

## 📋 Usage

1. Make the main script executable:
   ```bash
   chmod +x menu.sh

